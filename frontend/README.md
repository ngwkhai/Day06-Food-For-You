# Food For You Frontend

React/Next.js frontend for the Food For You recommendation experience.

## Run locally

```bash
npm install
npm run dev
```

The app runs at:

```text
http://localhost:3000
```

## Environment

Copy `.env.example` to `.env.local` when connecting to the backend.

